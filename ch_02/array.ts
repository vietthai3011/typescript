let hobbies = ["Swim", "Cooking"];

// hobbies.push(10);

let users: Array<string | number>;

users.push(10);
users.push("10");

let possibleResult: [number, number] = [1, 2];

// possibleResult = [1, 23, 4];

let user = {
    name: "Thai",
    age: 22,
};

type User = typeof user

let val: Object = 2;

let data: Record<string, number | string>;

data = {
    name: "thai",
    age: 22,
};



